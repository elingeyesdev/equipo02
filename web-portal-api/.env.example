PORT=3001
JWT_SECRET=cambiar-esto-en-local
DATABASE_PATH=./data/portal.db

# URL del api-middleware (modelo universal /datos, /solicitudes, auditoría…)
API_MIDDLEWARE_URL=http://localhost:3000

# Cuentas de la Consola BaaS (tenant + rol → X-API-Key inyectada en /admin/api/*)
USUARIOS_ADMIN_FILE=./config/usuarios-admin.yaml
